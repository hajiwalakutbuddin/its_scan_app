extends layout

block content
  h1 Scan Card for Attendance
  form#scanForm(action="/scan", method="POST")
    input(type="text", name="cardId", placeholder="Scan or enter Card ID", autofocus required)
    button(type="submit") Submit
  div#feedback
  a(href="/report") View Report
